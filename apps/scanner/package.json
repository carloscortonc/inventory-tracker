{
  "name": "@inventory-tracker/scanner",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20.16.0"
  },
  "scripts": {
    "start": "ts-node-esm src/index.ts",
    "build": "rimraf dist && tsc && tsc-alias -f && copyfiles .env package*.json Dockerfile dist",
    "deploy": "rpi deploy dist --name scanner --vars SRV_PASSWORD=$SRV_PASSWORD --build-on-target"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "copyfiles": "^2.4.1",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.5.3"
  }
}
